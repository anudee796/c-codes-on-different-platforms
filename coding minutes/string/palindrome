#include<iostream>
using namespace std;
int main(){
    char ch[20];
    cin.getline(ch,20,'\n');
    int len=strlen(ch);
    int i=0,j=n-1;
    int index=0;
    while(i<j){
        if(ch[i]!=ch[j]){
            index++;
        }
        i++;
        j--;
        
    }
    if(index==0){
        cout<<"palindrome"<<endl;
    }else{
        cout<<"not palindrome"<<endl;
    }
}
